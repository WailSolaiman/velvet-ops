/**
 * App routing links (react-router-dom). For same-page sections use `<a href="#section-id">`.
 * Replaces `next/link` imports from the section specs.
 */
/* eslint-disable react-refresh/only-export-components -- re-exports for stable import path */
export { Link, NavLink, useNavigate, useLocation } from 'react-router-dom'
export type { LinkProps } from 'react-router-dom'
